import React from "react";
import { Jumbotron } from "react-bootstrap";

const Header = () => (

    <Jumbotron>
        <h1>React Quiz</h1>
        <p>
            Creating components, using pre-built components, applying state, lifting state and routing.
        </p>
    </Jumbotron>

)

export default Header;